name: Discourse Plugin

on:
  push:
    branches:
      - main
  pull_request:

jobs:
  ci:
    uses: discourse/.github/.github/workflows/discourse-plugin.yml@v1
    with:
      # Pinned to the core commit running on terrytrilla.club (2026.9.0-latest).
      # Bump together with the forum upgrade, never ahead of it.
      core_ref: c124ff3e49fd3810cc7453cb714571e48a3d7744
